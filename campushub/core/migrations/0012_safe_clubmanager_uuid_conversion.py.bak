# Generated by Django 6.0.4 on 2026-05-08 00:00

from django.db import migrations, models
import uuid


class Migration(migrations.Migration):

    dependencies = [
        ('core', '0011_remove_clubclaim_club_remove_clubclaim_user_and_more'),
    ]

    operations = [
        # First, create a new UUID column
        migrations.AddField(
            model_name='clubmanager',
            name='temp_id',
            field=models.UUIDField(default=uuid.uuid4, editable=False, unique=True),
        ),
        # Generate UUIDs for existing records using a deterministic approach
        migrations.RunSQL(
            """
            UPDATE core_clubmanager 
            SET temp_id = uuid_generate_v5(
                uuid_ns_url, 
                CONCAT('clubmanager-', id::text)
            );
            """,
            None,
        ),
        # Drop the old primary key constraint
        migrations.RunSQL(
            "ALTER TABLE core_clubmanager DROP CONSTRAINT core_clubmanager_pkey;",
            None,
        ),
        # Drop the old unique constraint
        migrations.RunSQL(
            "ALTER TABLE core_clubmanager DROP CONSTRAINT unique_root_per_club;",
            None,
        ),
        # Rename the temporary column to replace the old one
        migrations.RunSQL(
            "ALTER TABLE core_clubmanager RENAME COLUMN temp_id TO id;",
            None,
        ),
        # Add back the primary key constraint
        migrations.RunSQL(
            "ALTER TABLE core_clubmanager ADD PRIMARY KEY (id);",
            None,
        ),
        # Add back the unique constraint
        migrations.RunSQL(
            "ALTER TABLE core_clubmanager ADD CONSTRAINT unique_root_per_club UNIQUE (club_id) WHERE role = 'ROOT';",
            None,
        ),
    ]
