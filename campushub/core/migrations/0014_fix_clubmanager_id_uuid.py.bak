# Generated by Django 6.0.4 on 2026-05-08 00:00

from django.db import migrations, models
import uuid


def convert_clubmanager_ids(apps, schema_editor):
    # We get the model from the versioned app registry;
    # avoids direct model import which is unstable.
    ClubManager = apps.get_model('core', 'ClubManager')
    
    # Generate UUIDs for existing records
    for manager in ClubManager.objects.all():
        manager.id = uuid.uuid5(uuid.NAMESPACE_URL, f'clubmanager-{manager.id}')
        manager.save(update_fields=['id'])


class Migration(migrations.Migration):

    dependencies = [
        ('core', '0013_merge_0012'),
    ]

    operations = [
        # First, alter the field to allow UUIDs
        migrations.AlterField(
            model_name='clubmanager',
            name='id',
            field=models.UUIDField(default=uuid.uuid4, editable=False, primary_key=True, serialize=False),
        ),
        # Then run the data migration to convert existing IDs
        migrations.RunPython(convert_clubmanager_ids, migrations.RunPython.noop),
    ]
